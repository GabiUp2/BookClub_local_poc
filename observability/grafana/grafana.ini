; Grafana configuration for local observability
; British English comments per repo rules

[auth.anonymous]
; Allow anonymous viewing locally; restrict in production
enabled = true
org_role = Viewer

[paths]
provisioning = /etc/grafana/provisioning
